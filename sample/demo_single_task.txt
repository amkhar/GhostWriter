Standup Meeting — 2024-01-15 (Monday)

Attendees: Alice, Bob, Carol

---

Alice:
- Yesterday: Worked on the API rate limiter.
- Today: Continuing rate limiter.
- Blockers: The `get_session` function in `session.py` silently drops expired
  sessions — there's still no log line when a session expires. I flagged this
  last standup and the one before. It makes debugging logouts impossible.

Bob:
- Yesterday: Reviewed PRs.
- Today: Docs cleanup.
- Blockers: None.

Carol:
- Yesterday: CI fixes.
- Today: More tests.
- Blockers: Agreeing with Alice — the missing session-expiry log line in
  `session.py` keeps coming up and never gets done.

Action items:
- [ ] Add a session-expiry log line in `session.py` (raised in 3 standups, still unassigned)
